generator client {
  provider = "prisma-client-js"
}

datasource sqlite {
  provider = "sqlite"
  url      = "file:dev.db"
}

model Sertificate {
  id Int @default(autoincrement()) @id
  sertificateId Int @unique
}
